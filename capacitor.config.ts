import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gemma.foodapp',
  appName: 'food-management-app-frontend',
  webDir: 'dist/FoodManagementApp-frontend/browser'
};

export default config;
